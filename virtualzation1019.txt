云计算的特点
1.起大规模
2.虚拟化构架
3.高可用性
4.高扩展性

云计算关键技术
1.分布式计算 2.并行计算 3.效用计算 4.网络存储 5.负载均衡 6.虚拟化 7.高可用

虚拟化，是指通过虚拟化技术将一台计算机虚拟为多台逻辑计算机。在一台计算机上同时运行多个逻辑计算机，每个逻辑计算机可运行不同的操作系统（Guest OS），并且应用程序都可以在相互独立的空间内运行而互不影响，从而显著提高计算机的工作效率。这样的逻辑计算机称为虚拟机。

虚拟化和云的关系
1.虚拟化是云计算中最主要的支撑技术之一。
2.云计算将各种IT资源以服务的形式通过互联网交付给用户。而虚拟化使IT基础设施的资源部署和分配更灵活。
3.虚拟化不是云计算中必须的，但他使云计算如虎添翼，能为用户提供更优质的服务。

Hypervisor  
Hypervisor是一种运行在物理计算机和虚拟机操作系统之间的中间软件层，它可允许多个虚拟机操作系统或应用共享同一套基础物理硬件，因此也可以看作是虚拟环境中的“元”操作系统。
它可以协调访问服务器上的所有物理设备和虚拟机，也叫虚拟机监视器（Virtual Machine Monitor）
Hypervisor
Hypervisor可以直接运作在裸机上，称为“裸金属架构”，其本身作为一个操作系统，使用并管理底层的硬件资源，对上层运行的虚拟机提供资源调用接口。这一类的代表产品有VMware ESX Server、Citrix XenServer和Microsoft Hyper-V，以及Linux下开源的KVM等。
Hypervisor也可以运作在宿主操作系统上，以一个应用程序的方式运行，称为“宿主型架构”。它利用宿主操作系统提供的设备驱动和底层服务来进行虚拟机的内存管理、进程调度和资源管理等。这样虚拟机对硬件的访问必须经过宿主操作系统，因而带来了额外的性能开销。典型的代表是VMware Workstation、Microsoft Virtual PC、Oracle Virtual Box等。

深信服桌面云方案=桌面云一体机VDS+云终端aDesk（瘦客户要）

VDS：1.VMP虚拟化管理平台 2.虚拟存储VS 3.虚拟化桌面接入管理系统VDC



++++++++++++++++++
FC存储
1. FC（Fibre Channel，光纤通道）协议是SAN（Storage Area Networks，存储区域网络）中使用的一种数据传输协议。SAN可以分为FC SAN和IP SAN，其中FC SAN使用FC协议族，而IP SAN使用TCP/IP协议族（例如iSCSI协议）。
2. 在FC SAN中，通信的双方，通常一端为服务器，另一端为磁盘设备。服务器与磁盘设备之间可以通过光纤直接连接在一起（点对点架构），也可以通过FC交换机间接连接在一起（交换式架构）
3. 为了保证FC存储服务的安全性，通常会将物理上连通的FC SAN网络划分为多个VSAN，实现不同服务器之间的隔离访问。同时一个VSAN内的磁盘资源，又可以进一步划分为多个Zone，分别指派给不同的Port，达到访问控制的目的。
4.服务器如果需要连接到FC SAN使用存储资源，需要配置一块FC HBA卡。HBA卡通常插在服务器的PCI或PCI-E插槽上，然后通过光纤连接到FC存储或FC交换机。

+++++++++++++++++++++
SANGFOR_aCloud

管理管理口： 集群主机之间通过管理口进行通信。如果管理通信口异常，会导致主机离线，影响配置同步和虚拟机运行。因此建议管理通信口使用独立的交换机连接。

数据通讯口：  数据通信口用于集群内虚拟机之间跨主机通信时传输流量，经过物理网络是会进行VXLAN封装。建议数据通信口也使用独立的交换机连接。VXLAN是虚拟可扩展局域网，是一种overlay的网络技术，使用MAC in UDP的方法进行封装。
（ 数据通信口默认配置为复用集群管理口，为了获得较高的网络转发性能，建议将“管理通信口”和“数据通信口(vxlan)”设置到不同的网口。）

物理出口：用来连通虚拟网络和物理网络。“物理出口”对内可以连接虚拟路由器、虚拟网络设备或虚拟机，对外需要连接到主机的物理网口。

